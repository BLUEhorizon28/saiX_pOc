<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/header_start_color">

    <!-- Header Section (No changes needed here) -->
    <LinearLayout
        android:id="@+id/header_section"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/header_background"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingTop="32dp"
        android:paddingBottom="48dp">

        <ImageView
            android:layout_width="73dp"
            android:layout_height="73dp"
            android:src="@drawable/app_logo" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="SAI Prerak"
            android:textColor="@color/white"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Khoje Har Khel Prerak. Har Gaon, Har Shehar."
            android:textColor="@color/white"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- Main Content Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/header_section"
        android:layout_marginTop="-24dp"
        android:background="@drawable/main_content_background"
        android:orientation="vertical"
        android:padding="16dp">

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <!-- UPDATE: Added gravity to this LinearLayout to center everything inside it -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_horizontal"
                android:orientation="vertical">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Standardized Tests"
                    android:textColor="@color/text_primary"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:layout_marginBottom="12dp"/>

                <!-- The GridLayout is now perfectly centered because of its parent's gravity -->
                <GridLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:columnCount="2"
                    android:useDefaultMargins="true">

                    <include
                        android:id="@+id/cardHeightWeight"
                        layout="@layout/test_card" />
                    <include
                        android:id="@+id/cardVerticalJump"
                        layout="@layout/test_card" />
                    <include
                        android:id="@+id/cardShuttleRun"
                        layout="@layout/test_card" />
                    <include
                        android:id="@+id/cardSitUps"
                        layout="@layout/test_card" />
                    <include
                        android:id="@+id/cardEndurance"
                        layout="@layout/test_card" />
                    <include
                        android:id="@+id/cardMoreTests"
                        layout="@layout/test_card" />
                </GridLayout>
            </LinearLayout>
        </ScrollView>
    </LinearLayout>

    <!-- Bottom Navigation Bar (No changes needed here) -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/card_background"
        app:menu="@menu/bottom_nav_menu" />

</RelativeLayout>